apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sbom-service-chart.fullname" . }}-test-connection"
  labels:
    {{- include "sbom-service-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sbom-service-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
